# direnv configuration
# Run `direnv allow` to enable

# Load .env (creates from .env.example if missing)
dotenv_if_exists .env

# Add local bin to PATH for development
PATH_add bin

# Display loaded config
log_status "cprr dev environment"
